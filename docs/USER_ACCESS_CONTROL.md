# 用户、角色与权限管理

管理员登录后进入「系统维护 → 用户与权限」。先在「角色管理」创建角色并勾选菜单和操作，再在「用户管理」添加用户名、初始密码并分配角色；也可以给单个用户额外授权。用户权限是所有已分配角色与单独授权的并集。

用户名使用3至64位字母、数字、点、横线或下划线；密码至少10字节、最多72字节。密码仅保存 bcrypt 哈希，不提供查询明文密码的接口。需要修改密码时使用「重置密码」。内置环境管理员保留为维护入口，不在此页面编辑或删除。

菜单权限提供对应页面及必要关联数据的读取能力；新增、编辑、删除、发布、下载、设备控制等操作另行授权。勾选操作时同时勾选其所属菜单。界面隐藏没有权限的按钮，后端按实际路由再次校验。一个页面涉及多个接口时，权限列表可能提供多个操作，例如普通问答和流式问答。智能助手、运行总览、接入测试和巡检具有其业务所需的关联数据读取范围，不是设备级数据隔离。

修改角色权限后，后续 API 请求立即使用新配置；前端重新进入窗口或刷新后更新菜单和按钮。修改用户、停用用户、重置密码使原登录令牌失效，用户需重新登录。删除后重新创建同名用户不会恢复旧登录。正在使用的角色不能删除；不能删除内置管理员或当前登录账户。已签发的 MQTT 订阅令牌最长15分钟有效，API会话与消息订阅令牌不能互用。

权限按当前租户隔离。数据库新增 `platform_access` 表，由已有 PostgreSQL 启动迁移自动创建。每个租户保存独立的用户、角色及递增修订号；写入使用修订号比较，遇到并发更新返回409，需刷新后重试。Redis/ClickHouse包装层透传统一仓储接口，不把账户保存在浏览器或临时文件中。开发用内存仓储提供同样接口，但不跨进程保存。

接口：

| 接口 | 用途 |
|---|---|
| `POST /api/v1/auth/login` | 内置管理员或新增用户登录 |
| `GET /api/v1/auth/me` | 当前身份及有效权限 |
| `GET /api/v1/access/permissions` | 可分配菜单、操作目录 |
| `GET/POST /api/v1/access/users` | 用户列表、新增用户 |
| `PUT/DELETE /api/v1/access/users/:id` | 编辑、删除用户 |
| `POST /api/v1/access/users/:id/password` | 重置密码并撤销旧登录 |
| `GET/POST /api/v1/access/roles` | 角色列表、新增角色 |
| `PUT/DELETE /api/v1/access/roles/:id` | 编辑、删除角色 |

配置入口属于管理员权限。受委托的管理用户也必须拥有「用户与权限」菜单及对应管理操作授权；授予用户或角色管理权限意味着允许其分配其他业务权限，应由负责账户管理的人员持有。

## 2026-09-14 本机验证

- `go test ./internal/httpapi -run TestAccessControlLifecycleAndIsolation -count=1`：用户/角色生命周期、bcrypt、跨租户隔离、菜单与操作拒绝、权限目录所有操作的未授权请求、密码重置、停用、删除后重建、并发修订以及消息令牌不能访问HTTP API。
- `node iot_front/tests/browser/access-management-check.mjs`：本机真实 PostgreSQL、Redis/ClickHouse 仓储路径，页面创建角色和用户，实际登录，菜单和按钮差异，接口403，补授权后生效，重置密码后旧会话401；临时测试账户清理。
- `node iot_front/tests/browser/shell-management-check.mjs`：菜单分组折叠、16个菜单正常加载、表格按钮边框和背景；使用浏览器内45条样本验证协议分页，不写入平台协议数据。
- 前端单元测试覆盖45条协议分页、末页、缩小数据集后页码修正；保留设备三分组和类型筛选回归。

本记录是本机验证，未代表生产环境验收。
