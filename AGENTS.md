# 消防 IoT 平台协作与开发规范

本文件适用于本仓库及其子目录，面向参与开发的人和 AI。协作规则、关键业务约束和工作入口均在仓库内保存；不依赖个人知识库、外部记忆目录或历史会话。新环境只需取得本仓库，即可按当前任务开展工作。

下文路径均相对仓库根目录。具体接口和操作细节查对应源码及仓库文档；本文件不承担功能完成清单或运行环境验收记录的职责。

## 1. 协作与任务范围

- 默认中文，先说结果，再给必要证据；解释长度按任务需要，避免固定风险清单和无关实现细节。
- “帮我做、优化、修复”等按行动请求执行到约定结果；常规可逆实现按现有约定推进，不只给计划或反复询问是否继续。
- 当前用户明确要求决定任务目标。已有授权在任务内延续；只有缺少影响正确性或授权的关键信息时才提问，同时完成不受影响的工作。
- 用户补充要求时保留已完成工作；状态问答后继续原任务，除非用户明确停止或改变目标。
- 保持任务范围，不顺带迁移架构、替换技术栈、清理无关代码或执行历史待办。文档中的建议、示例和旧计划不构成新的执行授权。
- 本地规则与用户明确要求冲突时，遵循用户要求；若某条规则确实导致停下或改变方向，指出具体文件、原文和适用原因，不凭泛化风险新增审批。
- 有效信息在同一任务内复用；先按问题定位资料，不默认通读所有文档、日志或历史。
- 交付说明实际改动、验证结果和与本次任务有关的未验证范围；需要用户操作时给出可复制命令及执行目录。

## 2. 事实依据与文档维护

- 当前源码、配置和本次实测用于判断实现现状。README、设计文档、覆盖表、旧报告只能提供入口或背景，不能单独证明功能已经运行或验收。
- 遇到资料冲突，核对对应实现及测试；不能仅因某份记录更新较晚、措辞肯定或被多次引用就判定正确。未决问题写清影响范围，继续独立工作。
- 长期协作约束维护在本文件；接口、配置、部署及业务流程的详细说明维护在 `docs/` 或 README，同一细节尽量只保留一处完整正文。
- 改动使既有说明失效时同步更新。只保存下次仍有用的决定、边界与可定位证据，不复制整段会话、完整终端输出或临时排错过程。
- 有必要留存验证记录时，标明日期、环境、命令和结论，区分已验证、待确认与历史结果。历史成功不能写成本次重新验证。
- 不要求读取或更新仓库外的个人知识库；外部笔记缺失不影响开发。用户另行提供的历史资料仅按本次相关性参考。
- 不记录密码、令牌、私钥、真实环境连接串或含秘密的输出。运行数据和环境凭据不由本文件备份。

## 3. 项目定位与源码入口

平台为独立 Go API 与 Vue 管理端，覆盖设备接入、协议源码发布、报文归档与回放、规则告警、摄像头元数据、知识库、AI 辅助运维及备份管理。保持独立平台方向，不重新引入已移除的 ThingsPanel 集成，也不把 JetLinks Java 协议包作为平台依赖。

| 路径 | 职责 |
|---|---|
| `cmd/iot-platform/` | API 启动与依赖装配入口 |
| `internal/httpapi/`、`internal/auth/` | HTTP 接口、认证、角色与租户边界 |
| `internal/core/`、`internal/model/`、`internal/ports/` | 业务编排、领域模型和依赖接口 |
| `internal/adapters/` | 数据库、消息、对象存储、AI 等外部实现 |
| `internal/parser/` | 报文解析 |
| `internal/protocolbuild/`、`internal/protocolruntime/`、`internal/protocolworker/` | Go 协议源码构建、版本运行与 Worker 契约 |
| `internal/mcpserver/` | 平台 MCP 工具与访问边界 |
| `internal/config/`、`internal/deploycheck/`、`internal/metrics/` | 配置、部署检查和指标 |
| `internal/backup/`、`cmd/backup-service/` | 备份逻辑与独立备份服务 |
| `protocol-packages/gb26875-dahua/` | 可独立维护的完整 Go 协议 module 示例 |
| `cmd/gb26875-gateway/`、`cmd/gb26875-virtual-device/`、`cmd/loadgen/` | 专用网关、虚拟设备和负载工具；按任务使用 |
| `iot_front/` | Vue 3、Vite 管理端，复用现有 Element Plus 和样式体系 |
| `scripts/`、`compose*.yaml`、`deploy/` | 本地准备、部署、离线打包和服务配置 |

修改业务时沿“页面 / API → 业务逻辑 → 依赖接口 → 存储或外部适配器”追踪完整链路。多个页面出现同类症状，优先查共享 API、公共状态和仓储实现。

## 4. 环境与日常运行

- 在实际 Git 根目录执行仓库命令，不假定父目录也是仓库，不自动创建父仓库或子模块。
- Go 版本以 `go.mod` 为准；Node 要求和可用脚本以 `iot_front/package.json` 为准。沿用项目依赖与锁文件，非必要不升级或更换包管理器。
- 本地、在线、离线是三套独立配置：本地为 `compose.local.yaml` / `.env.local`；在线为 `compose.yaml` / `.env.online`；离线为生成包中的 Compose 配置 / `.env.offline`。
- 首次准备、构建和部署可能启动容器或下载依赖，只在任务需要时运行；单纯修改代码或文档不必重启整套服务。

Windows 首次本地准备，在仓库根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\setup-local.ps1
```

Linux / macOS 对应入口：

```bash
bash ./scripts/setup-local.sh
```

准备好配置和依赖后，终端一在仓库根目录启动 API：

```powershell
go run ./cmd/iot-platform --env-file .env.local
```

终端二启动前端，以下两行依次执行：

```powershell
cd iot_front
npm run dev
```

- Windows 遇到 npm 执行策略问题时使用 `npm.cmd`。前端日常入口为 `http://localhost:5173`；Vite 默认代理 API 到 `http://localhost:8081`，可用 `VITE_API_PROXY_TARGET` 覆盖，具体见 `iot_front/vite.config.js`。
- GoLand 后端工作目录设为仓库根目录，运行 `cmd/iot-platform`，程序参数设为 `--env-file .env.local`；WebStorm 前端工作目录设为 `iot_front`，运行 npm 的 `dev` 脚本。
- 进程环境变量优先于环境文件；排查配置时注意 IDE 遗留变量。不给用户复制硬编码凭据到 IDE 的配置方案。
- 宿主机连接依赖须使用宿主机可达地址及已发布端口；Compose 服务名供容器内部使用。Linux 虚拟机依赖 / Windows 源码模式通过准备脚本的 `--dependency-host` 和 `--api-host` 指定地址，不固定某台机器的 IP。
- 项目介绍和功能范围查 README；启动、停止、升级及远程依赖细节查 `docs/TECHNICAL_DETAILS.md` 和 `docs/DEPLOYMENT.md`。在线部署入口为 `scripts/deploy-online.*`，离线打包与部署入口为 `scripts/package-offline.*`、`scripts/deploy-offline.*`。

## 5. 后端、接口与数据约束

- 沿用已有分层、命名、错误响应和 API 契约；优先复用公共能力，不在页面或 HTTP handler 中复制已有业务逻辑。
- 涉及字段、状态、分页或筛选时，同时检查后端模型、持久化、查询、API 和前端调用。已有字段名优先保留；确需数据库变更时提供与实际存储匹配的可执行迁移及必要说明。
- 读取、写入、下载、回放和关联校验均保留租户隔离及角色检查，不能只依赖前端隐藏按钮。不要直接信任请求体中的租户或跨资源 ID。
- 原始报文链路保持 `RawMessage → 原始归档 / 幂等索引 → 内部消息队列 → Parser → StandardMessage → 存储 / 规则 / 告警` 的职责划分。原文详情、下载及回放使用 `rawMessageId`，不可与标准消息 ID 混用。
- 原始报文使用 PostgreSQL / ClickHouse 分层存储，具体路由及阈值查 `internal/adapters/rawstore/`；MinIO 用于备份制品及旧对象兼容读取，不恢复逐条原始报文上传 MinIO 的旧方案。
- 只有成功解析的数据才能对外发布解析结果；失败保留未解析状态和可追溯原文，不能伪造标准消息或将内部原始队列当作对外解析通道。
- 设备主动上报 `ALARM_REPORT` 即使没有匹配规则，也应保留设备来源告警；匹配规则时沿规则链路处理，不自动创建测试规则。确认、恢复和关闭须正确重算设备状态及剩余活动告警。
- 摄像头只管理元数据和设备关联：单个摄像头最多关联一个设备，设备可以关联多个摄像头。视频由外部平台提供，不擅自恢复平台拉流、代理、预览或旧多对多方案。
- 备份操作沿用服务端权限及凭据边界；备份存在、下载成功和恢复成功是不同结论，按本次实际操作表述。

## 6. 协议开发与版本发布

- 当前自定义协议入口为独立 Go 源码包。通用 JSON、路径映射、固定长度 HEX / 校验和 / 固定偏移外的新专用协议，优先由外部 Go 包维护，不继续向平台堆叠内置厂商解析器。
- 保留现有 Modbus 等兼容能力；历史支持不等于继续扩展旧路线的授权。新接入参考 `docs/GO_PROTOCOL_PACKAGES.md` 和独立协议示例。
- Go 源码上传经过校验、构建、样例试跑再发布；保留源码和制品哈希、不可变版本、产品绑定及回滚链路。构建或样例失败不得替换当前有效版本。
- 第三方依赖随 `vendor` 上传；构建关闭 CGO 与在线依赖下载，不执行上传包里的任意构建脚本。继续校验路径穿越、链接、解压大小、平台、超时及输出限制。
- `go-protocol-v2` 的 ingress、decode、encode 契约以协议文档和运行时实现为准；TCP 半帧、粘包、UDP 完整帧、设备识别、应答和命令关联必须分别处理。
- 同连接版本切换尊重完整帧及待应答命令边界；原始报文归档帧前状态和实际协议版本，解析与回放使用同一快照。失败发布与回滚不能破坏历史版本。
- Worker 是具有服务进程操作系统权限的子进程，超时和最小环境变量不等于强隔离沙箱。不要把逐报文 Worker 描述为常驻边缘采集。中心 TCP 主动连接与定时查询由接入实例调度，细节见 `docs/TCP_CHILD_DEVICE_ACCESS.md`；不把跨副本会话路由或任意连接事件回调视为已有能力。

## 7. AI、知识检索与 MCP

- AI 工作流走 Harness；告警自动研判涉及 `Engine.AI` 和 Provider。Harness 健康不能单独证明告警 Provider 可用，排查时核对实际调用链和进程加载的配置。
- AI 模型切换复用现有模型管理与 Provider 配置，避免新增重复前端入口。默认模型、兼容接口地址和开关查当前部署配置，不把本机配置写成所有环境的固定前提。
- AI 规则草稿默认禁用，经用户确认后启用；协议助手保存映射草稿，不绕过完整协议源码校验及发布流程。
- 知识文档上传与检索保留租户及 Agent / `workflowId` 归属；不支持范围隔离时拒绝无范围检索，不静默回退到全库检索。
- 业务专用 Agent 与聊天 Agent 保持用途区分；内置 Manifest 只读，动态 Agent 按现有管理接口操作，知识配置集中在知识库页。
- 平台 Harness 使用受控 MCP 只读工具与精确权限校验，禁用 shell、文件系统、jobs、goal、skills、subagent 和设备控制等越界工具。此条限制的是产品内 Harness，不是开发助手的全局工具权限。
- 内部服务令牌、短期 MCP JWT 和浏览器登录凭据用途分开；租户、用户和会话边界由后端校验，不把服务端秘密或完整敏感工具结果传给浏览器。

## 8. 前端交互

- 保持中文、简洁、清爽、低噪声；复用现有组件和样式，按钮小而清楚，优先解决真实任务路径，不堆叠重复入口。
- 页面内容区、弹窗正文独立滚动；检查窄屏导航、表格溢出及固定列遮挡。全局告警展示设备名称、内容、类型、时间、等级，内部 ID 主要用于导航和关联。
- 存储与逻辑保留标准状态代码，界面显示中文含义；AI 风险等级兼容大小写，不通过修改后端代码值解决翻译问题。
- 处理好加载、失败、空数据、重复提交和刷新后的状态；长任务展示实际进度，不能把估算进度写成已经完成。
- 流式消息避免每个分片触发重渲染，结束后安全渲染 Markdown；浏览器缓存按租户和用户隔离，切换身份不能泄露旧会话内容。
- UI 问题优先复现真实操作；能运行浏览器时验证修复后的交互，仅源码检查通过时不得声称已完成浏览器验收。

## 9. 修改与验证流程

1. 开始编辑前检查 `git status --short`，识别用户或其他任务已有修改；不覆盖、不回滚无关工作。
2. 按问题查找相关源码、调用方和既有测试，优先使用 `rg`；先定位共同原因，再完成影响范围内的修改。
3. Go 改动使用 `gofmt`，沿用现有风格；接口或配置行为变化同步更新仓库说明。避免无关格式化、大规模搬移文件和生成物污染。
4. 先运行有针对性的检查；缺陷回归覆盖真实失败场景和关键边界，不为简单文档或低影响外观修改编写复述实现的测试。
5. 按影响范围运行完整测试或构建。已通过后，无新增改动、失败或疑点时不反复执行同一组检查。
6. 交付前复核 diff 和状态，说明本次改动及实际验证；已有失败与本次引入失败分开记录。

常用检查命令：

| 执行目录 | 命令 | 适用范围 |
|---|---|---|
| 仓库根目录 | `go test ./internal/core ./internal/httpapi` | 示例：业务及 API 相关包；按改动选择实际包 |
| 仓库根目录 | `go test ./...` | 后端完整测试 |
| `protocol-packages/gb26875-dahua` | `go test ./...` | 独立协议 module；根 module 测试不能代替此项 |
| `iot_front` | `npm test` | 现有前端测试 |
| `iot_front` | `npm run build` | 前端构建 |
| 仓库根目录 | `git diff --check` | 空白错误检查，不能代替功能验证 |

- 部署脚本改动检查 PowerShell / Bash 对应入口、环境变量、Compose 渲染和失败路径。现有冒烟入口为 `scripts/tests/deployment-smoke.ps1` 与 `scripts/tests/deployment-smoke.sh`，须提供可用的独立 Compose 可执行文件路径，具体参数见脚本。
- 部署冒烟使用真实 Compose 配置解析及模拟 Docker / HTTP 操作；通过不代表已构建镜像、下载模型或部署目标服务器。
- 文档修改通常只需核对内容、路径、命令和 diff，不必启动应用或运行全套测试。环境缺失时如实说明未运行内容，不能伪造通过。
- 源码检查、单元测试、模拟集成、真实中间件联调、真实设备和生产验收分别表述；`/health/live` 正常不能独立证明数据库持久化、模型检索或设备链路成功。

## 10. Git、部署与数据保护

- 只修改本次相关文件；不随意删除用户未跟踪文件，不使用破坏性 reset / clean 撤销未知改动。提交、推送或部署按用户任务授权执行，不能把普通本地编辑扩大为远程发布。
- 配置示例可提交，真实 `.env`、数据目录、构建输出和离线包按仓库忽略规则管理。不要为方便验证而读取并输出全部环境秘密。
- 既有 Compose 项目名、数据卷及凭据具有运行状态，修改配置文件不等于数据库密码已同步更新；升级须沿用正确环境，避免误建空卷后误判数据丢失。
- 删除数据卷、清理数据库、注销 WSL、删除或移动 VHDX 等操作先核对明确目标、损失范围和可恢复性，未经任务授权不得执行；日常启停不使用卷清理代替。
- 脚本兼顾项目目标 Windows / Linux 环境，不随意使用目标机不支持的新命令。部署改动需要区分脚本模拟验证、镜像构建和目标机实际执行。
- 本文件随仓库版本管理，可跟随仓库克隆和备份保留规则；本地修改只有在提交并同步至远端或其他备份后才有异机恢复副本。业务数据库、对象存储和凭据仍需各自备份。

## 11. 仓库内参考文档

- [README](README.md)：炬联项目介绍、功能、架构和平台对比。
- [技术详情](docs/TECHNICAL_DETAILS.md)：环境准备、本地 / 在线 / 离线入口、扩展模块及开发检查。
- [部署配置与维护](docs/DEPLOYMENT.md)：配置、端口和维护操作。
- [离线部署](docs/OFFLINE_DEPLOYMENT.md)：离线包、模型、校验和更新。
- [设备协议接入流程](docs/设备协议接入流程.md)：设备与产品接入。
- [Go 协议包](docs/GO_PROTOCOL_PACKAGES.md)：当前源码上传、运行时、发布和回滚契约。
- [GB26875 协议](docs/GB26875_DAHUA_V103.md)：独立协议及 TCP / UDP 接入。
- [AI 工作流](docs/AI_PLUGIN_HARNESS.md)：Harness、Agent、MCP 及安全边界。
- [协议 V2 设计](docs/DEVICE_PROTOCOL_ACCESS_DESIGN.md)：设计背景，使用前核对当前实现。

文档缺失或不一致时，按当前需求与源码推进并修正文档；不得退回要求个人知识库存在才能继续的工作方式。
